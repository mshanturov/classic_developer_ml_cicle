services:
  model-api:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["uvicorn", "src.api.app:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    volumes:
      - ./:/app
    ports:
      - "8012:8000"
